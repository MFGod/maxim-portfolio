export type WorldBounds = {
  minX: number;
  maxX: number;
  minZ: number;
  maxZ: number;
};

/**
 *  Границы мира после обрезки.
 *
 *  Согласованы на живой сцене 2026-08-21 отступами от края ландшафта:
 *  запад 975, восток 1370, север 1235, юг 1115. Ландшафт был 2465 x 2465,
 *  стал 119.7 x 114.7 — и ни один из 8968 объектов не вышел за границу.
 *
 *  Обрезка визуальная, не ради fps: за пределами этих границ лежали всего два
 *  меша на 255 треугольников из 7 041 310 в сцене, и far plane камеры (350)
 *  отсекал их и без того.
 *
 *  Запас до ближайшего объекта: запад 11.8, восток 21.5, север 8.7, юг 7.5.
 *  Двигать границы внутрь дальше — значит резать контент. По востоку порог
 *  жёсткий: за 1391 уходит целая локация с двумя благодатями.
 */

export const MAP_BOUNDS: WorldBounds = {
  minX: -48.019,
  maxX: 71.72,
  minZ: -76.584,
  maxZ: 38.156,
};

/**
 *  Поверхность океана в мировых координатах.
 *
 *  Меш воды сдвинут на y +2.88, его две большие грани лежат на -3.62 и -2.79
 *  в локальных: слэб толщиной 0.83, верх которого и есть уровень моря.
 *  Под ним, на -0.63..-3.6, лежит непрозрачный зелёный `Sand Global` — дно.
 *  Оно и красит море в бирюзу, просвечивая сквозь воду с прозрачностью 0.6,
 *  поэтому убирать его нельзя: без дна море станет плоско-синим.
 */
export const SEA_LEVEL = 0.09;

/**
 * Абсолютный минимум высоты камеры — над водой, где под ногами нет рельефа.
 * Запас маленький: над сушей камеру держит карта высот (`collision.js`), а
 * прежние +1.2 были выше рельефа зелёных низин и не давали спуститься к земле.
 */
export const CAMERA_FLOOR = SEA_LEVEL + 0.2;
