import { deepFreeze } from '@/lib/freeze';

import type { WorldBattle } from '@/lib/world/battle';

/**
 * Стычки: где на карте дерутся живые с нежитью.
 *
 * Площадки не выбраны на глаз. Стычке нужен чистый круг ровной земли радиусом
 * около 0,7 юнита — две линии выхода и место схода между ними, — а на этой
 * карте таких мест мало: суша занимает малую долю габарита, и почти вся она под
 * кустом, камнем, склоном или постройкой. Из 24 327 точек карты, просеянных
 * шагом 0,75, годными оказались 46.
 *
 * Просев идёт по трём слоям, и каждый из них обязателен — первая укладка
 * проверяла только два и поставила все три стычки в воду:
 *
 * 1. **Земля.** Материалы травы, грунта и песка (33 789 треугольников из 3,7
 *    миллиона) разложены по клеткам тем же способом, что и лента дорог у
 *    дозоров: замер за микросекунды вместо луча в десятки миллисекунд.
 * 2. **Вода.** Отдельный слой, и по имени материала его не найти — оно пустое.
 *    Узнаётся по цвету (`46d3dd`) и прозрачности: 235 треугольников, лежащих
 *    на восемнадцати высотах — море на 0,091, озёра Лиурнии на 2,197, пруды
 *    выше. Без этой проверки площадка «на песке» оказывается дном залива, а
 *    бойцы стоят по колено в воде.
 * 3. **Луч по видимой поверхности.** Земля в этой карте продолжается под
 *    постройками: у стен Лейндела под ногами честная трава, а над ней ярус
 *    террасы. По индексу площадка чистая, в кадре бойцы стоят внутри лестницы.
 *    Так отпали четыре лучших по всем прочим меркам точки — расхождение луча и
 *    индекса доходило до 4,4 юнита.
 *
 * Кусты преградой не считаются: сквозь листву дерутся, а вокруг ствола уже нет.
 * У каждой площадки записан наклон плоскостью — поляны ровные, но не идеально,
 * а при росте фигуры 0,117 перепад в три сантиметра на краю строя уже виден.
 *
 * Фронт развёрнут поперёк взгляда с ближайшего ракурса маршрута: со станции бой
 * виден в профиль, обе шеренги сразу, а не затылок передней.
 *
 * Стычек в мире две, и это не выбор, а итог перебора: `__world.battles.scan()`
 * прошёл карту шагом полюнита и нашёл ровно две точки, где полоса под бой и
 * суха, и ровна, и свободна, и не накрыта постройкой. Обе на равнине Кейлида —
 * остальная суша либо под водой, либо в кустах, либо под ярусом Лейндела.
 * Расширение поиска до сорока пяти юнитов от маршрута камеры ничего нового не
 * дало: дело не в удалении, а в том, что ровной пустой земли на этой карте
 * почти нет.
 *
 * Сдвиги фазы разведены намеренно: два боя в одном кадре не должны сходиться и
 * падать в ногу — тогда это читается как заведённый механизм, а не как жизнь.
 */

/** Рост бойца в юнитах мира — как у людей, расставленных автором карты. */
const HEIGHT = 0.117;

export const worldBattles: WorldBattle[] = deepFreeze([
  {
    /**
     * Равнина Кейлида среди упавших колонн — одна из двух площадок, что нашлись
     * на всей карте. Земля здесь плоская до миллиметра, воды нет, над головой
     * ничего: девять лучей вдоль полосы дали расхождение ноль. Фронт развёрнут
     * не поперёк взгляда, а вдоль: поперёк полоса упирается в стену развалин —
     * луч показал зазор в 5,28 юнита.
     */
    id: 'стычка-колонны',
    at: [19, 0.245, 1.4],
    slope: [0, 0],
    facing: 0,
    offset: 0,
    undead: {
      models: ['skeleton_minion', 'skeleton_warrior', 'skeleton_mage'],
      height: HEIGHT,
    },
    living: { models: ['rogue', 'knight', 'mage'], height: HEIGHT },
  },
  {
    /**
     * Вторая площадка той же равнины, в семи юнитах к северо-западу. Фронт
     * развёрнут поперёк взгляда с прибытия к «Собственным проектам»: здесь
     * полоса чистая в обе стороны, и со станции бой виден в профиль.
     */
    id: 'стычка-гниль',
    at: [15, 0.245, 7.4],
    slope: [0, 0],
    facing: 1.946,
    offset: 23,
    undead: {
      models: ['skeleton_warrior', 'skeleton_rogue', 'skeleton_mage'],
      height: HEIGHT,
    },
    living: { models: ['knight', 'barbarian', 'mage'], height: HEIGHT },
  },
]);
